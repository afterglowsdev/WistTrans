pluginManagement {
    repositories {
        // Android Gradle Plugin lives in Google's Maven repository.
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = "WistTrans"

