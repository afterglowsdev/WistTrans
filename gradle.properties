# Enable AndroidX (required since this project depends on AndroidX libraries).
android.useAndroidX=true

# Keep Jetifier on for safety in case any transitive dependency is still support-lib based.
android.enableJetifier=true

